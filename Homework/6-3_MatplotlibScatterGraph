# 3-3. Matplotlib 을 이용해 산점도 그래프 그리기
# 데이터 변경

import matplotlib.pyplot as plt
import numpy as np

# 주어진 데이터
np.random.seed(0)

n = 50
x = np.random.rand(n)
y = np.random.rand(n)

# 1. Matplotlib 을 이용해 그래프 그리기
plt.scatter(x, y)

# 2. 그래프 제목 , 축 라벨 추가
plt.title("연도별 매출 변화")
plt.xlabel("년도")
plt.ylabel("매출 (단위: 백만)")

# 3. 그래프 출력
plt.show()